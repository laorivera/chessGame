#include <iostream>
#include "move.h"
#include "board.h"

  Board boardCopy;
    
  void Move::addBoard(Board b) {
      boardCopy = b;
  }

  void Move::movePiece(int fromX, int fromY, int toX, int toY) {
  }

